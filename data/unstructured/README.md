# Unstructured sample inputs

Sample inputs go here (placeholder).

Examples of unstructured sources (no real candidate data needed yet):
- resume files (`*.pdf`, `*.docx`)
- free-text recruiter notes (`*.txt`)

TODO: Add tiny anonymized sample files for local testing.
